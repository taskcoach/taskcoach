TaskCoach on Debian Bookworm - Quick Start
===========================================

This is a simplified guide to get TaskCoach running on Debian 12 Bookworm.

IMPORTANT: PEP 668 on Bookworm
-------------------------------

Debian Bookworm protects system Python from accidental breakage (PEP 668).
This means `pip install --user` will give an error. This is GOOD!

Solution: We use system packages + a virtual environment (venv).


OPTION 1: Automated Setup (Recommended)
----------------------------------------

1. Run the setup script:

   ./setup_bookworm.sh

2. That's it! The script will:
   - Install system packages (wxPython, numpy, lxml, twisted, etc.)
   - Create a virtual environment at .venv (in the project directory)
   - Install remaining packages (desktop3, lockfile, gntp, distro, pypubsub)
   - Generate required resources (icons, templates)
   - Create a launch script (taskcoach-run.sh)
   - Test the installation

3. Start TaskCoach:

   ./taskcoach-run.sh


OPTION 2: Manual Setup
-----------------------

1. Install system packages:

   sudo apt-get update
   sudo apt-get install -y python3-wxgtk4.0 python3-six python3-twisted \
       python3-lxml python3-numpy python3-dateutil python3-chardet \
       python3-keyring python3-pyparsing python3-pyxdg python3-venv

2. Create virtual environment (in the taskcoach directory):

   cd /path/to/taskcoach
   python3 -m venv .venv
   source .venv/bin/activate
   pip install desktop3 lockfile gntp distro pypubsub
   deactivate

3. Generate resources:

   cd /path/to/taskcoach/icons.in
   python3 make.py

   cd /path/to/taskcoach/templates.in
   python3 make.py

   cd /path/to/taskcoach

4. Create launch script:

   cat > taskcoach-run.sh << 'EOF'
   #!/bin/bash
   SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
   source "$SCRIPT_DIR/.venv/bin/activate"
   cd "$SCRIPT_DIR"
   python3 taskcoach.py "$@"
   EOF
   chmod +x taskcoach-run.sh

5. Run TaskCoach:

   ./taskcoach-run.sh


Testing Your Installation
--------------------------

Run the test suite:

   ./test_taskcoach.sh

This will verify:
- Python version compatibility
- System packages installed correctly
- Virtual environment packages working
- Resources generated correctly
- Application can start


Common Usage
------------

# Start with GUI:
./taskcoach-run.sh

# Open a specific file:
./taskcoach-run.sh mytasks.tsk

# Use custom settings file:
./taskcoach-run.sh --ini=/path/to/settings.ini

# Use different language (e.g., French):
./taskcoach-run.sh --language=fr

# Show all options:
./taskcoach-run.sh --help


Advanced: Headless Testing (Optional)
--------------------------------------

NOTE: Skip this section if you're running on a normal desktop!

This is ONLY for:
  • Automated testing / CI/CD
  • Running over SSH without X11 forwarding
  • Headless servers

1. Install xvfb:
   sudo apt-get install -y xvfb

2. Generate resources with xvfb:
   cd /path/to/taskcoach/icons.in
   xvfb-run -a python3 make.py

   cd /path/to/taskcoach/templates.in
   xvfb-run -a python3 make.py

   cd /path/to/taskcoach

3. Run TaskCoach headless:
   xvfb-run -a ./taskcoach-run.sh


Understanding the Setup
-----------------------

TaskCoach uses TWO sources for Python packages:

FROM DEBIAN REPOS (system packages):
  ✅ python3-wxgtk4.0 - GUI framework
  ✅ python3-twisted - Networking
  ✅ python3-lxml - XML processing
  ✅ python3-numpy - Numerical arrays
  ✅ python3-six - Python 2/3 compatibility
  ✅ python3-dateutil - Date handling
  ✅ python3-chardet - Character encoding detection
  ✅ python3-keyring - Keyring access
  ✅ python3-pyparsing - Parsing library
  ✅ python3-pyxdg - XDG standards

FROM PYPI (in virtual environment):
  📦 desktop3 - Desktop integration
  📦 lockfile - File locking
  📦 gntp - Growl notifications
  📦 distro - Linux distribution detection
  📦 pypubsub - Publish-subscribe messaging

This approach:
  • Uses stable Debian packages where possible
  • Isolates PyPI packages in a safe venv
  • Prevents breaking system Python
  • Is the recommended best practice


Troubleshooting
---------------

Error: "error: externally-managed-environment"
→ This is PEP 668 protection. Use the setup script or create a venv.

Error: "No module named 'wx'"
→ Install: sudo apt-get install python3-wxgtk4.0

Error: "couldn't import icons.py"
→ Run: cd icons.in && python3 make.py && cd ..
→ If headless: cd icons.in && xvfb-run -a python3 make.py && cd ..

Error: "No module named 'taskcoachlib.persistence.xml.templates'"
→ Run: cd templates.in && python3 make.py && cd ..
→ If headless: cd templates.in && xvfb-run -a python3 make.py && cd ..

Error: "Unable to access the X Display"
→ You're either running over SSH or headless
→ See "Advanced: Headless Testing" section

Error: "Virtual environment not found"
→ Run: ./setup_bookworm.sh


System Requirements
-------------------

- Debian 12 (Bookworm)
- Python 3.11+ (included in Bookworm)
- ~100 MB disk space (with venv)
- 256 MB RAM minimum


What's Tested and Working
--------------------------

✓ Python 3.11 compatibility
✓ wxPython 4.2.0 integration
✓ All core dependencies
✓ GUI startup and rendering
✓ Task creation and management
✓ File loading and saving
✓ Import/Export functionality
✓ PEP 668 compliance (using venv)


Getting Help
------------

- Full setup guide: DEBIAN_BOOKWORM_SETUP.md
- README: README.md
- GitHub: https://github.com/taskcoach/taskcoach
- Website: http://www.taskcoach.org


Quick Example
-------------

After setup, try this:

   # Start TaskCoach
   ./taskcoach-run.sh

   # In the GUI:
   1. File → New to create a new task file
   2. Task → New Task to add your first task
   3. Enter a task description
   4. File → Save As to save your tasks

   # Or from command line with existing file:
   ./taskcoach-run.sh example.tsk


Why Virtual Environment?
-------------------------

Debian Bookworm implements PEP 668 to prevent:
  × Breaking system Python packages
  × Conflicting with system tools
  × Accidental package downgrades
  × Security vulnerabilities

Using a venv is:
  ✓ Safe and isolated
  ✓ Easy to remove (just delete the taskcoach directory)
  ✓ Standard Python best practice
  ✓ What Debian recommends
  ✓ Keeps everything in one place

The taskcoach-run.sh script automatically activates the venv,
so you don't have to think about it!


Version Info
------------

TaskCoach: 1.5.0
Tested on: Debian 12 (Bookworm)
Python: 3.11.2+
wxPython: 4.2.0+
Last Updated: 2025-11-15 (Updated for PEP 668)
